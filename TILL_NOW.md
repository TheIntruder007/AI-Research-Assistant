# Till Now

**Overall completion: ~100%** (all four pipeline services, the orchestrator, the terminal app, and both the synchronous and asynchronous/streaming backend API built and verified live; full-pipeline integration test coverage and review docs complete; three Service 1/Service 2 reliability passes complete — Service 1 🟢, Service 2's citation-boundary problem 🟢 verified across 3 topics; **productized as an installable `pip install researchgenie` product with a one-time setup wizard, automatic Ollama readiness, an optional Gemini provider, and a premium terminal UI** — see DECISIONS.md D-022 and `analytics_and_results.md`; **paper length/completeness fixed and real IEEE/Springer LaTeX format support added**, verified across 4 real end-to-end runs — see DECISIONS.md D-025/D-026 and `analytics_and_results.md` §15)

## ✅ Completed components
- Project workspace, now located at `E:\Agentic AI\AI-Research-Assistant` (moved 2026-08-27 from `Desktop\AI-Research-Assistant`; `.venv` was rebuilt fresh at the new path since venvs bake in an absolute path).
- Git repository (local history intact through the move), `.gitignore` in place.
- Core documentation: `README.md`, `PROJECT_NOTES.md`, `DECISIONS.md`, `TILL_NOW.md`.
- Hardware inspected (i7-13700HX, 16GB RAM, RTX 4060 8GB VRAM) and recorded.
- Ollama installed and running, model storage at `D:\ollama_models` (unaffected by the project move — see DECISIONS.md D-008).
- Local AI model selected and **verified working**: `qwen3.5:9b`.
- GitHub CLI installed but not authenticated — user chose to hold off on GitHub setup for now.
- Project Python venv (`.venv`) with `httpx`, `pydantic`, `python-dotenv`, `pytest`, `langgraph` + langchain-core stack, plus service-specific deps (`pypdf`, `defusedxml`, `PyYAML`).
- `shared/utilities/llm_provider.py` — local-first LLM abstraction (Ollama `/api/chat`, streaming JSON, tiered context sizing that accounts for actual prompt length, not just output length — see DECISIONS.md D-010).
- `shared/contracts/discovery_contract.py` / `writing_contract.py` / `verification_contract.py` / `qa_contract.py` — Pydantic input/output contracts for all four services.
- **Research Discovery Service — fully working end-to-end** (`services/research-discovery/`): query generation → multi-source literature search (Semantic Scholar, OpenAlex, PubMed, arXiv — all keyless) → dedupe/rank → full-text mining → gap & novelty analysis. Reliability fixes in DECISIONS.md D-009. `PaperMetadata`/`Paper` carry the raw `abstract` text through to the contract (needed by Services 2 and 4).
- **Research Writing Service — built and tested** (`services/research-writing/`): adapted a LangGraph-based outline-driven writing agent (DECISIONS.md D-010). Takes a `DiscoveryResult`, builds an outline + one literature file per selected paper, runs the writing graph against the local model, and produces a `WritingResult`. A real structural bug found via full-corpus timing runs (per-gap literature-review subsections guaranteed near-total section failure) was fixed and re-verified — see DECISIONS.md D-011.
- **Citation Verification Service — built and tested** (`services/verification/`): adapted the DOI/URL-verification piece of a full-stack literature-review app (DECISIONS.md D-012), swapping its paid Scopus-primary check for a keyless Crossref-then-OpenAlex check. Classifies each rendered reference as verified / invalid / unverifiable — never silently inventing or repairing a bad reference.
- **Quality Assurance Service — built and tested** (`services/quality-assurance/`): adapted the deterministic citation-audit and evaluation-reflection logic of a larger multi-agent review system (DECISIONS.md D-013). Takes Discovery + Writing + Verification output together and independently checks for uncited high-risk claims, cited claims that don't token-overlap their source's title/abstract (matched by DOI back to the original Discovery record), and required-section presence — producing a 0–5 quality score across four dimensions plus an overall score. Deliberately makes **no LLM calls** (grading a draft with the model that wrote it would be a weaker independence guarantee than deterministic checks, and this hardware is already the bottleneck elsewhere).
- **Full four-service pipeline verified live, chained end-to-end** twice — once by manually chaining the four services (`scripts/smoke_test_full_pipeline.py`'s original form), and again through the real orchestrator once built (see next item). Confirmed Service 3's DOI-extraction regex works against Service 2's real IEEE-formatted reference strings, and Service 4's scoring/limitation logic produces sensible output across different runs (3.5/5.0 and 4.2/5.0 overall, correctly reflecting each run's actual citation count) — see DECISIONS.md D-013's verification note.
- **Orchestrator and terminal app — built and verified live** (`orchestrator/pipeline.py`, `terminal_app/cli.py`, DECISIONS.md D-014): `run_pipeline()` runs Services 1→2→3→4 in sequence for one `ResearchRequest`, persists each stage's structured result under `outputs/<topic-slug>_<run-id>/NN_<stage>/result.json` plus a `final/` folder (draft, quality report, validation report, reference list) and `metadata.json`, and wraps every service's own ad hoc progress-event shape into one standard `{run_id, stage, service, status, emoji, title, message, details, timestamp}` format. `scripts/smoke_test_full_pipeline.py` now calls the orchestrator directly (rather than manually chaining services) and was re-run live end-to-end successfully — confirmed the full artifact layout persists correctly even on a run where Service 2 produced almost no citations (an expected instance of its known reliability limitation, not an orchestrator bug).
- **Backend API — built and verified live** (`orchestrator/api.py`, DECISIONS.md D-015): a FastAPI app with `POST /research` (validates a `ResearchRequest`, runs the pipeline via `run_pipeline()`, returns the final `PipelineResult`) and `GET /health`. Synchronous/blocking by design for this MVP — a run can take 15+ minutes, and per the project brief, event streaming is an explicit later step, not part of this pass. Unit tests use FastAPI's `TestClient` with the orchestrator faked out (success, `PipelineError` → 502, malformed request → 422); the server was also started live with `uvicorn` and confirmed to serve `/health` (200) and reject a bad `/research` body (422) over a real HTTP connection.
- **Full-pipeline live integration test added** (`tests/integration/test_full_pipeline_live.py`, DECISIONS.md D-016): runs the real orchestrator through all four services against the live model and live external APIs for a fresh full-size (6-paper) corpus, and asserts every per-stage `result.json` and every `final/` artifact actually exists and is contract-valid. Gated behind `AI_RESEARCH_RUN_FULL_PIPELINE=1` (skipped by default so the routine `pytest` run stays ~8 minutes) — ran successfully end-to-end in 913.01s (~15m13s).
- **Asynchronous/streaming API added** (`orchestrator/api.py`, `orchestrator/run_registry.py`, DECISIONS.md D-017): `POST /research/runs` starts a run in the background and returns a `run_id` immediately (202); `GET /research/runs/{run_id}` polls its status/result/error; `GET /research/runs/{run_id}/events` streams every progress event over SSE, ending with an `event: end` marker. The original synchronous `POST /research` is unchanged. Verified against a real running server: a real run's `run_id` was returned instantly, its status showed `running`, and its SSE stream carried real live Discovery-stage progress events. Found and fixed a real bug during that manual check — a leftover progress-message string from Service 1's adapted source repo said "with Claude" when this project only ever runs the local Ollama model; fixed to say "with the local model."
- **Reliability pass 1: Service 1 relevance gate + Service 2 targeted retry** (`discovery/relevance.py` new, DECISIONS.md D-018/D-019; full report in `analytics_and_results.md`): Service 1 gained a deterministic hard relevance gate + multiplicative relevance-weighted ranking, fixing the root cause of the previously-documented off-topic-paper bug (verified: a real re-run of the exact same query rejected a GINA asthma-management guideline and 8 other genuinely unrelated papers, with all 6 selected papers scoring 0.81–1.0 relevance). Service 2's literature-card retry is now targeted (prior validation errors are fed back into the retry prompt) instead of blind, and `WritingResult`/`DraftMetadata` gained an explicit `draft_status: "complete"/"partial"` classification. Real-run verification found the targeted retry mechanically works but does not yet fully resolve every case — a more precise root cause was identified (`TAG-ROOT`'s semantics are systematically as broad as the whole research question) and documented as a follow-up rather than fixed under this pass's risk budget. 14 new unit tests added (all fast, no live model needed); full regression suite (54 tests) still passes with zero regressions.
- **Reliability pass 2: Service 2 final pass** (DECISIONS.md D-020; full report in `analytics_and_results.md` §10): fixed the `TAG-ROOT` overlap at its root (prompt-level ancestor/descendant exclusion rules), fixed a real deterministic code bug (`write_next_section`'s container branch constructed `SectionDraft(content="", ...)`, violating the schema's own `min_length=1` on every run with a container-type tag — not a model-reliability issue at all), and found + fixed a third, higher-leverage issue while stress-testing: `FAST_REVIEW_CONFIG.max_section_revision_rounds=0` meant the already-correct audit-driven revision path never actually ran in production. Three real full-pipeline stress runs (2 topics) after all three fixes: one reached `draft_status: "complete"` (5/5 sections, QA 4.2/5.0), one reached 4/5 sections. Zero card-build failures and zero container-content crashes across all three runs. 12 new tests added; full regression suite (66 tests) passed with zero regressions. Verdict at the time: 🟡 partially improved — one residual citation-boundary failure remained (fixed in pass 3 below).
- **Reliability pass 3: Service 2 citation-boundary fix** (DECISIONS.md D-021; full report in `analytics_and_results.md` §12): traced pass 2's one remaining failure to its real root cause — **not** a genuine evidence-boundary leak as assumed. The model cited only allowed papers, but echoed the `@`-decorated `[@P001]` prose-placeholder form into the separate `cited_paper_ids` field (or, in a mirror-image variant, dropped the `@` from the prose placeholder itself), and the audit's set-equality comparisons treated the resulting string mismatch as an "invalid citation" — a false-positive detection bug. Fixed by normalizing `cited_paper_ids` at the schema boundary (`SectionDraft`/`SectionDraftContent`) and making the placeholder-extraction regex tolerant of a missing `@`, plus prompt clarifications. Verified directly against both real failed artifacts (now `passed: True` with no content changes) and via 8 new unit tests. **Then stress-tested across 3 real topics** (fasting/cognition re-run, remote work — the exact previously-failing topic, and a genuinely new domain: urban green space & mental health): **all 3 reached `draft_status: "complete"`, every section resolving on its first audit attempt, zero revisions needed.** Full regression suite (74 tests) passed with zero regressions. **Final honest verdict: 🟢 COMPLETE** for the citation-boundary problem this pass targeted — repeated real testing across multiple topics (including the exact previously-failing one) now consistently shows no citation-boundary failures.
- **Productization: `pip install researchgenie` (DECISIONS.md D-022; full report in `analytics_and_results.md` §13)**: the pipeline is now an installable product, not just a library. New `researchgenie/` package: local config (`~/.researchgenie/config.json`, never committed), a one-time setup wizard (AI engine choice, automatic Ollama detection/install/model-pull, or Gemini API key entry), automatic Ollama server startup, and a premium Rich-based terminal UI (one restrained accent palette, live section-grouped progress from real pipeline events, clean summary/completion/failure screens). Added an optional Gemini provider to `shared/utilities/llm_provider.py` (explicit, deliberate exception to the project's original local-only rule, made per this pass's explicit instruction — Ollama remains default/recommended). Added deterministic LaTeX export (`shared/utilities/latex_export.py`) with best-effort PDF compilation, closing a gap between the README's long-standing claims and actual pipeline output. Packaged with `pyproject.toml`; **verified via a real wheel build installed into a completely fresh, isolated venv** (the strongest test possible without a live PyPI publish) and **two real end-to-end `researchgenie` runs** — one a full research request completing successfully with a live progress display and real completion screen. 44 new tests; full regression suite (116 tests) passed with zero regressions. Honestly documented gaps at the time: Gemini untested against the live API (no key available), Windows/macOS Ollama auto-install untested against a genuinely-missing installation (this dev machine already has Ollama), PDF compilation untested (no LaTeX toolchain on this machine at the time — since installed, see next item), and no actual PyPI publish attempted.
- **Paper length and completeness fix + IEEE/Springer LaTeX templates (DECISIONS.md D-025/D-026; full report in `analytics_and_results.md` §15)**: `tectonic` is now actually installed on this machine (`C:\Users\owais\bin\tectonic.exe`), so PDF compilation is real and verified locally, not just in a Docker isolation test. Fixed the root causes behind short papers and an unreliable `draft_status`: the outline no longer duplicates the writing graph's own Introduction/Conclusion "bookend" calls (`writing_prep.py::build_outline()` now returns `Background/Literature Review/Discussion/Limitations`, D-025); a new `word_budget.py` turns a user-chosen length preset (Short/Standard/Detailed, now asked in both CLIs) into real per-section word targets/minimums/maximums; the generation prompts gained a balanced "develop fully when evidence supports it, never pad" instruction; a new `completeness.py` validates `draft_status` (`complete`/`partial`/**new** `failed`) from the ACTUAL RENDERED paper text instead of only the graph's internal signal; `markdown_to_latex()` now genuinely switches LaTeX document class by format (`IEEEtran` for IEEE, `llncs` for Springer — `svjour3` confirmed unavailable in this environment's LaTeX bundle). **Real end-to-end validation (4 fresh live-model pipeline runs, not just unit tests) found and fixed one new bug** (a childless "container" outline tag that rendered permanently blank, regardless of model luck) and **quantified a real pre-existing problem**: 3 of 11 historical runs had a silently-blank/replaced bookend section but were mislabeled `draft_status: "complete"` under the old logic — now correctly reported `partial`/`failed`. Average draft length rose from ~1,353 words (11-run historical baseline) to a 2,253-word post-fix average across 3 comparable real runs (~67% increase); one clean run reached 3,421/4,000 words (`on_target`). 34 new tests (word budget, under-generation, completeness, the container-tag fix, IEEE/Springer templates, CLI length prompts); full regression suite (153 tests) passed with zero regressions, including the real live-model integration test. Honestly documented: the bookend evidence-context audit's real replacement rate (11 of 14 runs checked) is now visible and correctly classified, not reduced — that was not this pass's goal.

## 🔄 Current working pipeline
- Service 1 (Research Discovery) — working and verified in isolation. The previously-documented relevance/ranking bug (an unrelated paper entering the corpus) is fixed via a deterministic hard relevance gate — re-verified against the exact same failure scenario. See DECISIONS.md D-018.
- Service 2 (Research Writing) — working; the structural bug that guaranteed near-total section failure on a full-size outline is fixed and re-verified (D-011). Literature-card retries are targeted (D-019); the `TAG-ROOT` overlap root cause and a real container-content code bug are fixed, and revision rounds are re-enabled (D-020); the citation-boundary false-positive bug is fixed and verified (D-021). **Latest stress runs: 3 of 3 real topics reached `draft_status: "complete"`, every section on its first attempt** — see DECISIONS.md D-021 and `analytics_and_results.md` §12 for the full evidence (verdict: 🟢 complete for the citation-boundary problem).
- Service 3 (Citation Verification) — working, verified in isolation and live-chained onto real Service 1→2 output (including a run with zero citations, handled gracefully). Reference-list-level checking only; positional in-text citation-marker verification is a documented future enhancement.
- Service 4 (Quality Assurance) — working, verified in isolation and live-chained onto real Service 1→2→3 output across multiple runs with different citation counts. Fully deterministic (no LLM calls).
- **Orchestrator** (`orchestrator/pipeline.py`) — working, verified live end-to-end; persists per-stage artifacts and standardized progress events. **`researchgenie` CLI** (`researchgenie/cli.py`) — the current primary product interface, calling the orchestrator directly in-process; the older `terminal_app/cli.py` remains in the repo (still tested — `tests/test_terminal_app_cli.py`) but is superseded as the product's front door.
- Review docs (`docs/reviews/review_2.md`, `review_3.md`) written. `GET /research/runs/{run_id}` + SSE event streaming built and verified live. Three Service 1/Service 2 reliability passes complete — Service 2's citation-boundary problem verified 🟢 complete across 3 real topics (see `analytics_and_results.md`).
- **Productized**: `pip install researchgenie` → `researchgenie` is now the real installation/launch story, verified via a real isolated-venv wheel install and real end-to-end runs — see DECISIONS.md D-022.

## 🐙 GitHub status
- **Remote set up 2026-08-28**: private repository `TheIntruder007/AI-Research-Assistant`, local history pushed as `origin/master`. `Sadiq8064` invited as a collaborator (write access, invitation pending acceptance). See DECISIONS.md D-006.

## 📊 Review milestone status
- **Review 1 (~33%): reached.** `docs/reviews/review_1.md` written. Workspace, local AI, and Service 1 are done and tested.
- **Review 2 (~66%): reached.** Services 1–3 built and verified.
- **Review 3 (~100%): reached.** All four services, the orchestrator, the terminal app, and the full backend API (synchronous and asynchronous/streaming) are built and verified live; full-pipeline integration test coverage and `docs/reviews/review_2.md`/`review_3.md` are complete. Three dedicated reliability passes fixed Service 1's relevance-selection root cause (🟢) and Service 2's draft-completeness/citation-boundary problem (🟢, verified across 3 real topics) — see `analytics_and_results.md`.

## ⚠️ Known problems / limitations
- This hardware (RTX 4060, 8GB VRAM) is slow for larger-context LLM calls on a 9B model. Service 2 now runs with **one** bounded section-revision round (`FAST_REVIEW_CONFIG.max_section_revision_rounds=1`, changed from 0 in D-020) — full-document revision (`max_full_revision_rounds`) remains 0 for this reason. See DECISIONS.md D-010/D-020.
- Leaf-section writing reliability in Service 2 is now well-verified: the `TAG-ROOT` overlap, the container-content crash, disabled revision rounds, and the citation-boundary false-positive bug are all fixed (D-019/D-020/D-021) — 3 consecutive real stress runs across 3 different topics (including the two previously-problematic ones) all reached `draft_status: "complete"` with every section resolving on its first attempt. This is not a claim of zero future failures on a 9B local model (irreducible variance remains), but the specific diagnosed bugs are closed and verified not to recur — see `analytics_and_results.md` §12.
- Failures still surface honestly via `WritingResult.draft_metadata.warnings`/`errors`/`draft_status` and are reflected in Service 4's `process_control` score, not hidden, on the rare occasion one does occur (e.g. "insufficient evidence" for a tag, or a bookend section being replaced after failing its own evidence-context audit — both correctly-functioning safety behavior, not defects).
- Service 1's relevance gate (D-018) is deterministic token/phrase overlap, not semantic understanding — it cannot distinguish two genuinely different meanings that happen to share a word. This is a documented, accepted limitation, not a defect; it closes the specific failure mode actually observed (zero topical overlap).
- Service 3 only checks the reference list, not positional in-text citation markers against the draft body — a documented scope decision (DECISIONS.md D-012), not a defect.
- Service 4's claim-to-evidence check depends on Service 3's extracted DOI matching a Discovery record; when Service 2's own leaf-section reliability issue leaves a draft with very few real citations (as in the live full-pipeline run — only 1 of the corpus's papers actually got cited), Service 4 has correspondingly little to check. This is expected given Service 2's known limitation, not a Service 4 defect.
- Semantic Scholar / OpenAlex-based verification searches intermittently fail with rate-limit-style errors in testing; the pipeline degrades gracefully in both cases.
- Recurring test-isolation issue: every pipeline service's entry point is named `service.py` and reached only via `sys.path` insertion, so a bare `import service` in a test file silently reuses whichever service module another test file imported first in the same pytest session. The orchestrator now imports all four in one process via `importlib.util.spec_from_file_location` under unique names up front (`orchestrator/pipeline.py::_load_service()`), which is also the fix pattern used per-test-file elsewhere; no longer purely an ad hoc per-occurrence patch now that the orchestrator centralizes it, though the test files still each do their own loading too.
- **Gemini provider**: a real security bug was found and fixed (D-023) — the API key leaked into tool output via URL-embedded query-parameter error chaining during the one live test attempted. Fixed (header-based auth, suppressed exception chaining, 2 regression tests); **a full successful research draft generated via Gemini has still not been observed** — the live test was halted for safety after the key exposure, before the original question (does content generation work) was answered. Needs a fresh, valid key to close.
- **Ollama automatic installation (missing-machine path)**: genuinely verified via Docker isolation for detection + the Linux safety-refusal guidance (D-024) — real, not mocked. Windows/macOS `winget`/`brew` install commands remain verified only via mocks (this dev machine already has Ollama; no Windows/macOS machine without it was available).
- **PDF compilation**: genuinely verified both via Docker isolation (D-024) and, since then, directly on the host machine — `tectonic` is now installed locally (`C:\Users\owais\bin\tectonic.exe`) and has produced real, valid PDFs (including both IEEE/`IEEEtran` and Springer/`llncs` document classes — D-026) across multiple real pipeline runs. `pdflatex` specifically remains untested (only `tectonic` has ever been used).
- No PyPI publish was attempted — both the wheel and the source distribution install correctly into fresh, isolated venvs (verified twice); making `pip install researchgenie` work from the public index requires actually uploading the package there, a user action, not a code change.
- **Paper length/completeness (D-025/D-026)**: the bookend Introduction/Conclusion evidence-context audit initially replaced content fairly often on this local 9B model (11 of 14 real runs checked) — first correctly surfaced via `draft_status`/`broken_sections` instead of silently hidden, then (D-027, same-day follow-up) its actual dominant root cause was found via live reproduction: the model occasionally wrote a citation range shorthand (`[P003-P006]`) the placeholder extractor couldn't parse at all, making a fully evidence-grounded section look like it cited papers never mentioned in the prose. Fixed (prompt clarification + tolerant range-parsing in `citation_formatter.py`) and verified with 2 real same-topic re-runs showing **zero** bookend replacement afterward, vs. 3/3 broken immediately before the fix. Length planning gives the system real capacity to reach a chosen word target (demonstrated at 3,421/4,000 in a clean run, real-measured 9 PDF pages via `pypdf`) but cannot guarantee it when an unrelated audit check fails a section. Springer's `svjour3` LaTeX class remains unavailable in this environment; `llncs` is used and documented as the substitution — see `analytics_and_results.md` §15/§16 for full detail.

## ➡️ Next technical task
Core pipeline, orchestrator, terminal app, full backend API (sync + async/streaming), integration test coverage, review documentation, all three Service 1/Service 2 reliability passes, and productization (`pip install researchgenie`) are all complete. No blocking work remains. Optional, non-blocking follow-ups only:
1. Continue occasional stress testing across further topics as a matter of ongoing confidence-building — 3 consecutive clean runs is strong evidence but not infinite proof against a 9B model's irreducible variance.
2. Actually publish the package to PyPI when the user is ready (a publishing/account step, not further code).
3. Verify Gemini against a real API key, and Ollama auto-install against a genuinely clean machine, when such an environment is available.
4. Verify PDF compilation against a real LaTeX toolchain when one is available.
2. Per the project's own stated sequencing, packaging (`pip install researchgenie`) or any new feature work can now be considered, since Service 2 has reached the required reliability bar.
