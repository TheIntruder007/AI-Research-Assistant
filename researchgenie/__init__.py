"""ResearchGenie — a local-first AI research assistant.

This package is the installable product layer: local configuration, the
one-time setup wizard, automatic local-model readiness checks, and the
terminal application. The actual research pipeline (discovery, writing,
citation verification, quality assurance) lives in the existing
`orchestrator`/`services`/`shared` packages, unchanged by this layer.
"""

__version__ = "0.1.0"
