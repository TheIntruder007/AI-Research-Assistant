Write only a concise analytical bridge for the requested parent section in the supplied
`output_language`.

Use the parent's `direct_points` and the supplied direct-child summaries. Establish the
organizing principle that makes the child topics belong together: the scientific question,
tension, sequence, contrast, or scale change that connects them. Show how the children
partition the parent problem and why their relationship matters to the review question.

Do not summarize children one by one, re-expand their evidence, pre-empt their conclusions,
or announce a table of contents. Do not use sibling topics, outside knowledge, direct
quotations, page numbers, or papers and points outside the allowlists. Avoid generic bridge
language such as "the following sections discuss". Prefer one or two dense paragraphs whose
final sentence creates a logical entry into the first child topic.

Render citations only as supplied `[@P001]` placeholders. Return the prose, a parent-safe
summary of the organizing logic, and complete `cited_paper_ids` and `used_point_ids`.
